#Define the class Name
class Name:
    def __init__(selfy):
        selfy.firstname = "[no first name]"
        selfy.lastname = "[no last name]"

#Define the class Person
class Person:
    #Create a new instance of Person
    def __init__(selfy):
        #Person's default values
        selfy.name = Name()
        selfy.eyecolor = "[no eye color]"
        selfy.age = -1

        
        
